version: '3'

services:
  backend:
    build: .
    command: gunicorn ecommerce.wsgi
    # command: gunicorn ecommerce.wsgi --bind 0.0.0.0:8000
    ports:
      - 8000:8000
    volumes:
      - ./:/app
  # frontend:
  #   build:
  #     context: ./todo-app
  #   volumes:
  #     - build_folder:/frontend/build
  # nginx:
  #   build:
  #     context: ./nginx
  #   ports:
  #     - 80:8080
  #   volumes:
  #     - build_folder:/frontend

# volumes:
#   build_folder:
